---

version: "2"

services:

  app:
    restart: always

  proxy:
    networks:
      - default
      - proxies

  db:
    environment:
      - MYSQL_ROOT_PASSWORD=pass
      - MYSQL_DATABASE=db

networks:
  default:
    driver: overlay
  proxies:
    external: true

